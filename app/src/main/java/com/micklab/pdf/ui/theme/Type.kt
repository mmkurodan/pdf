package com.micklab.pdf.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type scale is fine for a utility app; kept separate so it is
// easy to override later (e.g. bundling a Japanese-optimized font family).
val Typography = Typography()
